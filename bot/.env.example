# Telegram Bot Configuration
BOT_TOKEN=your_bot_token_here

# Backend API
BACKEND_URL=http://localhost:3000

# WebApp URL (https required!)
# For ngrok development: this will be updated automatically by dev-scripts/update-env.js
# For production: your deployed webapp URL (e.g., https://statusstock.app)
# IMPORTANT: Must use HTTPS (Telegram WebApp requirement)
WEBAPP_URL=https://your-domain.com

# Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info

# AI Configuration (DeepSeek API for natural language product management)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Internal API Security (bot-to-backend auth)
INTERNAL_SECRET=your_internal_secret_here

# Session Encryption (AES-256-GCM)
# Generate with: openssl rand -base64 32
# If not set, sessions are stored in plaintext (not recommended for production)
SESSION_ENCRYPTION_KEY=
